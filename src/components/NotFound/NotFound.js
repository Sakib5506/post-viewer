import React from 'react';
import { Typography } from '@mui/material';

const NotFound = () => {
    return (
        <div>
            <Typography marginTop={'100px'} variant='h3' color={'red'} align='center'>404 Not Found</Typography>
        </div>
    );
};

export default NotFound;